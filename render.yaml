services:
  - type: web
    name: workflow-site
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    branch: main
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000